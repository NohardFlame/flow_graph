# flow-graph

Action-centric document extraction MVP.

## Run with Docker Compose

```bash
docker compose up -d
```

API: http://localhost:8000

All app config comes from `.env`; see `.env` (or its template) for required variables.
